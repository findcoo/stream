sudo: required

language: go

go:
 - 1.8.x

env:
 - DOCKER_COMPOSE_VERSION=1.14.0

services:
 - docker

before_install:
 - curl https://glide.sh/get | sh

install: make install

script: 
 - make run.kafka
 - make test
 - go build
